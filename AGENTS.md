# Personal Growth OS 开发约束

## 项目边界

这是一个本地、单用户、Streamlit + SQLite 的个人成长系统。当前产品事实以 `docs/CURRENT_PROJECT_AUDIT.md` 和 `docs/REQUIREMENTS_STATUS.md` 为准；目标需求以 `docs/02_PRD.md` 和 `docs/10_ROADMAP.md` 为准。

## 修改前

1. 先读本文件、PRD，以及与任务相关的页面、service、Schema、测试和技术文档。
2. 先搜索已有函数和表，默认增量扩展，不复制第二套实现。
3. 区分“当前已实现”“部分实现”“目标规划”，不要用文案伪装完成功能。

## Scope 与修改原则

- 用户要求修改 A 时，只改完成 A 所需的合理范围，不顺手重构无关模块。
- 默认最小合理修改，不删除未知文件，不复制 `v2`、`final` 等平行实现。
- 未经用户明确要求，不更换 Streamlit、SQLite 或当前主技术栈。
- 不因文档中的目标规划直接创建空页面、空表或伪实现。

## 架构边界

- `pages/` 负责 Streamlit 交互和反馈；新业务规则放入 `services/`。
- `database.py` 管理连接、事务、Schema 与兼容升级。
- 页面不要新增大段直接 SQL；Goals、Review、Books 的现有直连 SQL 应在相关功能迭代时逐步收敛到 service。
- 当前没有 HTTP API。只有出现真实多端或集成需求时才评估 API。
- 不为本地单用户场景引入微服务、队列、Redis 或云数据库。

## 数据安全

- `data/growth.db` 是用户真实数据，禁止删除、重建、清空或用于测试。
- 测试必须传入临时数据库路径。
- Schema 变更前先备份，并在旧结构副本上验证迁移；不得以清库作为升级方案。
- 删除习惯或书籍会级联日志，UI 必须说明影响并二次确认。
- 目标删除和每日模板停用必须保留历史任务可读性。
- 捕获转换、目标主记录与版本快照等跨记录操作必须使用事务。

## 核心业务不变量

- 同一每日任务模板在同一天最多生成一个任务实例。
- 停用每日模板不删除已生成任务。
- 睡眠可以同时作为每日任务和习惯，但统计不能重复相加。
- 任务状态、完成度、完成时间和实际分钟保持一致。
- Planner 和未来 AI 只提供建议，未经用户确认不修改关键数据。
- Quick Capture 转换失败时必须保留原始内容。
- 目标历史完成后，修改目标必须同时写入版本记录和原因。

## 验证与文档

- 至少验证正常路径、错误路径和关键边界；运行 `python -m pytest -q -p no:cacheprovider --basetemp tests/runtime`。
- 未实际运行的验证必须明确说明。
- 产品状态变化时同步 `docs/REQUIREMENTS_STATUS.md`、相关设计文档、README 和 CHANGELOG。
- 重要架构或业务决策更新 `docs/14_DECISIONS.md`。

完成定义：代码和迁移完成、应用实际运行、正常/异常/边界验收通过、旧核心流程回归通过、数据持久化已验证、相关文档已同步，才可以声明完成。

## 隐私与 AI

- 日记、目标、习惯和数据库备份均为敏感数据。
- 不硬编码密钥，不默认上传个人内容。
- 接入 AI 时明确发送字段、征得用户许可、允许关闭，并让用户预览重要写入。
